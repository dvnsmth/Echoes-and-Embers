data/

Owns: Read-only tables (no logic):

Items, enemies, species, classes, skills, buildings, regions, loot, shops, dialog keys

Asset manifests (IDs → asset paths from assets/)
Exposes: Static objects/JSON.
Depends on: —
Must not depend on: code.